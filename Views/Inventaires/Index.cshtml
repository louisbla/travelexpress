@model IEnumerable<Garderie.ViewModels.InventairesViewModels.IndexInventairesViewModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockMax)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StockActuel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StockMax)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockActuel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
                
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InventaireId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InventaireId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InventaireId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
