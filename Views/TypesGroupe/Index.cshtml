@model IEnumerable<Garderie.Models.TypeGroupe>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Libelle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libelle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TypeGroupeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TypeGroupeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TypeGroupeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
